---
export interface Props {
  tone?: "default" | "on-dark";
}

const { tone = "default" } = Astro.props;
---

<p
  class:list={[
    "text-[0.72rem] font-semibold uppercase tracking-[0.14em]",
    tone === "on-dark" ? "text-leaf" : "text-green-deep",
  ]}
>
  <slot />
</p>
